# Config Overview

## Purpose

`config` is the stable config module that re-exports the repo-root `io.ts`
config for the rest of the repo.

## Docs

- `../lib/overview.md`

## Layout

- `../../src/config/index.ts`: re-export boundary
- `../../src/config/index.test.ts`: repo-path and contract assertions
