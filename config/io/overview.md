# Config Overview

## Purpose

`config` is the stable workspace package that re-exports the repo-root `io.ts` config for other packages.

## Docs

- `./goals.md`
- `../../lib/io/overview.md`
- `../../lib/io/goals.md`

## Layout

- `../src/index.ts`: re-export boundary
- `../src/index.test.ts`: repo-path and contract assertions
